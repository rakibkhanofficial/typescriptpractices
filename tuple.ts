// Function to return a tuple containing a string and a number
function getUserData(): [string, number] {
    return ["John", 30];
}

// Test the function
const userData: [string, number] = getUserData();

console.log(`User data:`, userData); // Output: User data: [ 'John', 30 ]
